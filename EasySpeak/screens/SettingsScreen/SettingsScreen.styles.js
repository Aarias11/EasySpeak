import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    backgroundImage: {
        flex: 1,
    },
    container: {
        flex: 1,
        // backgroundColor: 'rgba(0, 0, 0, 0.8)', // Ensuring a black overlay
        justifyContent: 'center',
        alignItems: 'center',
    },
    text: {
        color: 'white',
    },
});
